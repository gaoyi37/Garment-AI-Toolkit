export function generateTitle(productName) {
    return `Optimized title for: ${productName}`;
}

export function generateSellingPoints(productName) {
    return [
        `Selling point 1 for ${productName}`,
        `Selling point 2 for ${productName}`,
        `Selling point 3 for ${productName}`,
    ];
}
